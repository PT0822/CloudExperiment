# 云计算技术期末课设项目解说与报告撰写指南

小组成员：2023112554 陈沛陶、2023112557 马志凯  
项目目录：`D:\云计算技术\期末课设`  
代码仓库：[PT0822/CloudExperiment](https://github.com/PT0822/CloudExperiment)

本文档的目标是：即使不登录华为云、不重新运行实验，也能看懂当前项目做了什么、每个任务如何满足任务书、报告应引用哪些截图和日志。

## 1. 总体完成状态

| 模块 | 任务书分值 | 当前状态 | 说明 |
|---|---:|---|---|
| 第一部分：云计算平台搭建 | 50 | 已完成 | 容器化、CCE、应用部署、Redis PVC、ConfigMap Volume、HPA 均已完成并留证 |
| 第二部分：Spark 大数据分析 A 路线 | 40 | 已完成 | A-0 Spark Operator + SparkApplication 已完成；A-1/A-2/A-3 分析和性能对比已完成 |
| 报告质量 | 10 | 待写报告 | 本项目已提供报告素材，最终仍需写 PDF |
| 附加题1：监控系统 | +5 | 已完成 | Prometheus/Grafana Dashboard 已部署并截图 |
| 附加题2：CI/CD 流水线 | +5 | 已完成 | GitHub Actions 自动构建、推送 SWR、更新 K8s Deployment 成功 |
| 附加题3：MQTT 边缘计算模拟 | +5 | 已完成 | MQTT broker、bridge、publisher、Redis 存储链路已跑通 |

最终结论：除“撰写报告 PDF、发送提交邮件、答辩展示”外，实验执行和证据收集已经完成。

## 2. 项目目录结构怎么读

```text
期末课设/
  app/                         本地前后端应用和 docker-compose
    backend/                   Flask API，连接 Redis
    frontend/                  Nginx 静态页面和反向代理
  k8s/                         第一部分 Kubernetes YAML
  spark/                       Spark 分析代码、Spark Job、SparkApplication、Spark 镜像 Dockerfile
  addons/                      三个附加题相关代码和说明
    monitoring/                Prometheus/Grafana 配置
    cicd/                      CI/CD 说明
    edge-mqtt/                 MQTT 云边模拟代码与 YAML
  scripts/                     本地/WSL 构建、推送、部署脚本
  evidence/                    本地证据目录，报告截图和日志主要从这里取
  report/                      报告相关素材目录
  .github/workflows/           GitHub Actions 流水线
  项目解说_报告撰写指南.md      本文件
```

重要提醒：`evidence/` 目录是本地证据库，通常不会提交到 GitHub，但给队友写报告时应连同整个文件夹一起打包发送。

## 3. 第一部分：云计算平台搭建如何满足要求

### 3.1 任务1：应用容器化

任务书要求：后端多阶段 Dockerfile、requirements 至少新增一个包、前端首页含学号姓名、本地 docker compose 联调、镜像推送 SWR。

本项目对应文件：

| 内容 | 文件 |
|---|---|
| 后端 Dockerfile 多阶段构建 | `app/backend/Dockerfile.backend` |
| Python 依赖，额外包 requests | `app/backend/requirements.txt` |
| Flask 后端 API | `app/backend/app.py` |
| 前端 Dockerfile | `app/frontend/Dockerfile.frontend` |
| 前端页面，包含两位成员信息 | `app/frontend/static/index.html` |
| 本地联调 | `app/docker-compose.yml` |
| 构建推送脚本 | `scripts/build_push.sh` |

后端提供三个主要接口：

| 接口 | 作用 |
|---|---|
| `/api/ping` | 返回服务状态、项目名、两位成员学号姓名、UTC 时间 |
| `/api/visit` | Redis 中 `visit_count` 自增，用于证明 Redis 正常和持久化 |
| `/api/redis-test` | 写入并读取 `testkey=hello`，用于持久化验证 |

报告可引用证据：

| 证据 | 路径 |
|---|---|
| SWR 镜像列表截图 | `evidence/screenshots/2个镜像F&B.png` |
| 创建镜像截图 | `evidence/screenshots/创建2个镜像.png` |
| 前端访问证明 | `evidence/screenshots/前端证明1.png`、`evidence/screenshots/前端证明2.png` |

报告写法建议：说明 backend 使用 Python Flask + Redis，frontend 使用 Nginx 静态页面并反向代理 `/api/` 到后端；镜像推送到华为云 SWR，后续 CCE 从 SWR 拉取。

### 3.2 任务2：CCE 集群搭建

任务书要求：华为云 CCE，Kubernetes 版本 >= 1.27，2 个 Worker Ready，截图含 VERSION。

当前完成情况：

- CCE Standard 集群。
- Kubernetes 版本 `v1.33.10-r0-33.0.24`，满足 >= 1.27。
- 2 个 Worker 节点 Ready。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| CCE 节点 Ready 截图 | `evidence/01_cce_nodes_ready.png` |
| 集群创建运行截图 | `evidence/screenshots/集群创建并运行.png`、`evidence/screenshots/集群创建并运行2.png` |
| 节点创建截图 | `evidence/screenshots/节点创建.png` |
| 最终 kubectl 状态 | `evidence/final_verification_after_push_retry.txt` |

### 3.3 任务3：应用部署

任务书要求：后端 Deployment 副本 2，Redis 副本 1，Service 暴露后端/前端，ConfigMap + Secret 注入 Redis 配置，resources requests/limits。

本项目对应 YAML：

| 资源 | 文件 | 关键点 |
|---|---|---|
| ConfigMap + Secret | `k8s/01-configmap-secret.yaml` | `REDIS_HOST=redis-svc`，Secret 保存 Redis 密码 base64 |
| Redis PVC | `k8s/02-pvc.yaml` | `storageClassName: csi-disk`，10Gi |
| Redis Deployment | `k8s/03-redis.yaml` | 1 副本，`/data` 挂载 PVC，内存 limit 512Mi |
| Backend Deployment | `k8s/04-backend.yaml` | 2 副本，SWR 镜像，resources，ConfigMap/Secret 注入 |
| Frontend ConfigMap | `k8s/05-frontend-nginx-configmap.yaml` | Nginx 反向代理配置 |
| Frontend Deployment | `k8s/06-frontend.yaml` | ConfigMap Volume 挂载到 Nginx 配置路径 |
| Services | `k8s/07-services.yaml` | backend/frontend LoadBalancer，redis ClusterIP |
| HPA | `k8s/08-hpa.yaml` | backend min=1 max=4 CPU 60% |

最终服务公网地址：

| Service | 类型 | 公网 IP | 用途 |
|---|---|---|---|
| `frontend-svc` | LoadBalancer | `123.60.208.39` | 浏览器访问前端和 `/api/*` |
| `backend-lb-svc` | LoadBalancer | `120.46.193.84` | 直接访问后端 API |

最终接口验证保存在：`evidence/final_verification_after_push_retry.txt`。

其中 `/api/ping` 返回包含：

```json
{"service":"cloud-course-project","status":"ok","students":"2023112554 陈沛陶, 2023112557 马志凯"}
```

### 3.4 任务4：Redis 持久化存储

任务书要求：PVC Bound，写入数据，删除 Redis Pod，Pod 重建后数据仍存在。

实现方式：

- `k8s/02-pvc.yaml` 创建 `redis-data-pvc`。
- `k8s/03-redis.yaml` 将 PVC 挂载到 Redis `/data`。
- Redis 启动参数启用 AOF：`redis-server --appendonly yes`。
- `/api/visit` 使用 Redis `INCR visit_count`，删除 Redis Pod 后计数继续增长。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| PVC Bound 和资源状态 | `evidence/final_verification_after_push_retry.txt` |
| 删除 Redis Pod 前后 visit_count 继续递增 | `evidence/04_public_api_k8s_resources.txt`、`evidence/shot/cloudshell_资源状态_2026-06-16T20-14-33-868Z.png` |

### 3.5 任务5：ConfigMap Volume 挂载

任务书要求：Nginx 反向代理配置通过 ConfigMap Volume 挂载，而不是 envFrom；修改配置后 exec 查看文件更新。

实现方式：

- `k8s/05-frontend-nginx-configmap.yaml` 保存完整 `default.conf`。
- `k8s/06-frontend.yaml` 中：
  - `volumes.configMap.name = frontend-nginx-conf`
  - `volumeMounts.mountPath = /etc/nginx/conf.d/default.conf`
  - `subPath = default.conf`

报告可引用证据：

| 证据 | 路径 |
|---|---|
| ConfigMap 更新和 exec 查看 | `evidence/shot/cloudshell_ConfigMap更新证据_2026-06-16T20-21-07-058Z.png` |

报告要写的 2-3 句话：

- ConfigMap Volume 适合挂载配置文件，例如 Nginx 的 `default.conf`，应用可以按文件路径读取完整配置。
- `envFrom` 适合注入简单键值对，例如 `REDIS_HOST`、`REDIS_PORT`。
- 对于多行配置文件，Volume 挂载比环境变量更清晰，也更接近生产环境配置管理方式。

### 3.6 任务6：HPA 弹性伸缩

任务书要求：metrics-server 可用，HPA min=1 max=4 targetCPU=60%，压测扩容和停止后缩容截图。

实现方式：

- `k8s/08-hpa.yaml` 创建 `backend-hpa`。
- `metrics-server` 已 Running。
- 压测期间后端副本数扩展，停止后恢复。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| metrics 和 HPA 状态 | `evidence/05_hpa_metrics_verified.txt` |
| 压测扩容截图 | `evidence/shot/cloudshell_HPA压测运行中_2026-06-16T20-22-33-214Z.png`、`evidence/shot/cloudshell_HPA压测运行中_后续_2026-06-16T20-24-19-086Z.png` |
| 降容截图 | `evidence/shot/cloudshell_HPA降容与最终状态_2026-06-16T20-31-37-807Z.png`、`evidence/shot/cloudshell_HPA额外等待后状态_2026-06-16T20-37-45-220Z.png` |

报告分析要点：

- 扩容不是瞬时发生，因为 metrics-server 有采集周期，HPA controller 也有评估间隔。
- 缩容更慢，是因为 Kubernetes 默认有稳定窗口，避免短暂流量波动导致频繁伸缩。
- HPA 的价值是低负载时减少副本，节省资源券和云资源成本，高负载时自动增加副本提升可用性。

## 4. 第二部分：Spark 大数据分析如何满足要求

本项目选择任务书中的方向 A：Spark 大数据分析。

### 4.1 A-0 环境部署：Spark Operator + SparkApplication

任务书要求：安装 Spark Operator，通过 SparkApplication 提交 `wordcount.py`，等待 Driver Pod 和 Executor Pod Completed。

当前完成情况：

- 已安装 Spark Operator。
- 已解决 `ghcr.io` 镜像拉取问题：将 Spark Operator controller 镜像转推到华为云 SWR，并 patch controller 使用 SWR 镜像。
- 已构建自定义 Spark wordcount 镜像：`spark/Dockerfile.wordcount` 将 `spark/wordcount.py` 打入镜像。
- 已提交 SparkApplication：`evidence/spark_operator/sparkapplication_wordcount_keep_executor.yaml`。
- 已设置 `spark.kubernetes.executor.deleteOnTermination=false`，保留 Executor Pod，便于截图。

关键结果：

- SparkApplication `wordcount-demo` 状态为 `COMPLETED`。
- Driver Pod `wordcount-demo-driver` 状态为 `Completed`。
- Executor Pod `wordcountoperatordemo-...-exec-1` 状态为 `Completed`。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| Driver/Executor Completed | `evidence/spark_operator/wordcount_keep_executor_pods_final.txt` |
| SparkApplication 最终 YAML | `evidence/spark_operator/wordcount_keep_executor_sparkapplication_final.yaml` |
| Driver 日志和 wordcount 输出 | `evidence/spark_operator/wordcount_keep_executor_driver_logs.txt` |
| Spark Operator Running | `evidence/spark_operator/spark_operator_running_after_swr_patch.txt` |

`wordcount.py` 输出包含：

```text
2023112554    1
2023112557    1
cloud         2
kubernetes    2
spark         2
```

报告写法建议：说明由于 CCE 节点拉取海外 `ghcr.io` 镜像不稳定，采取“镜像转推到 SWR + imagePullSecrets”的方式保证 Spark Operator 稳定运行，这是实际云环境中常见的镜像源适配问题。

### 4.2 A-1 数据清洗

任务书要求：加载数据、打印 Schema 和前 5 行、缺失值比例、至少两种缺失处理策略、清洗前后行数、统计信息。

本项目对应文件：

| 内容 | 文件 |
|---|---|
| Spark 分析代码 | `spark/analysis.py` |
| 云端运行结果 | `spark_analysis_result.txt`、`evidence/07_spark_analysis_result.txt` |
| 数据集 | `douban_movies.csv` |

处理策略：

- 对关键字段 `rating_score`、`year` 使用 `dropna`，因为这些字段直接影响评分分析和时间趋势分析。
- 对文本字段 `summary`、`directors`、`countries` 使用 `fillna`，填充为“暂无简介”“未知导演”“未知地区”，避免分组统计时丢失过多记录。

报告可引用：`evidence/07_spark_analysis_result.txt` 中的 Schema、First 5 Rows、Missing Ratio、Raw rows、Clean rows、Numeric Summary。

### 4.3 A-2 Spark SQL 统计分析

任务书要求至少 4 个查询，包含 GROUP BY、ORDER BY Top-N、时间维度趋势、JOIN 或窗口函数。

本项目完成的 4 个查询：

| 查询 | 覆盖要求 | 代码位置 | 说明 |
|---|---|---|---|
| Query 1: Year Trend | 时间维度 + GROUP BY + ORDER BY | `spark/analysis.py` | 按年份统计电影数量和平均评分 |
| Query 2: Genre Top-N | GROUP BY + Top-N | `spark/analysis.py` | 展开电影类型，统计高评分类型 |
| Query 3: Country High Rating Top-N | GROUP BY + Top-N | `spark/analysis.py` | 统计高分电影国家/地区分布 |
| Query 4: Director Best Movie Window | 窗口函数 | `spark/analysis.py` | `row_number()` 选出每位导演最高分作品 |

报告需要对每个查询写不少于 50 字分析说明。可以从以下角度写：

- 年份趋势：哪些年份电影数量/评分变化明显，可能与数据集收录偏差和电影产业发展有关。
- 类型 Top-N：高评分类型可能更偏纪录片、剧情、历史等，说明观众评分与类型受众有关。
- 国家/地区高分：高评分电影数量既受电影产量影响，也受豆瓣用户群体偏好影响。
- 导演最佳电影：窗口函数适合“组内排序取第一”的问题，比普通 GROUP BY 更能保留整行信息。

### 4.4 A-3 性能对比与 Amdahl 分析

任务书要求：Pandas 单机 vs PySpark executorInstances=1 和 2，记录执行时间，绘制对比图，并结合 Amdahl 定律分析非线性加速原因。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| 性能数据 CSV | `evidence/spark_perf_compare/perf_summary.csv` |
| 性能对比图 | `evidence/spark_perf_compare/spark_perf_compare.svg` |
| 原始运行日志 | `evidence/spark_perf_compare/run.log`、`spark-perf-1.log`、`spark-perf-2.log` |

Amdahl 分析建议：

- Spark 有任务调度、Executor 启动、序列化、Shuffle、网络通信开销，所以 2 个 Executor 不会线性快 2 倍。
- 数据量不够大时，固定开销占比高，PySpark 的优势不明显。
- Pandas 单机在小数据集上可能更快，但 Spark 在数据量继续增大、内存不足或需要分布式容错时更有优势。
- 可以用公式 `S(p)=1/((1-f)+f/p)` 说明：可并行比例 `f` 越低，增加 Executor 带来的收益越有限。

## 5. 附加题完成情况

### 5.1 附加题1：监控系统

任务书要求：使用 kube-prometheus-stack 部署 Prometheus + Grafana，Dashboard 展示节点 CPU 利用率折线图、各 Pod 内存使用柱状图，并解释 Prometheus Pull 原理和至少 3 个指标。

当前实现：

- 已部署 Grafana。
- 已部署课程自定义 Prometheus 用于采集节点和 Pod 指标。
- Grafana Dashboard 已创建，展示节点 CPU 和 Pod 内存。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| Grafana Dashboard 截图 | `evidence/shot/grafana_dashboard_monitoring_viewport_2026-06-18.png` |
| 监控 Pod 状态 | `evidence/monitoring/monitoring_pods_final.txt` |
| 监控 Service 状态 | `evidence/monitoring/monitoring_services_final.txt` |
| Dashboard JSON | `evidence/monitoring/grafana_dashboard_payload.json` |

报告要解释的三个指标示例：

- `up`：Prometheus 对某个 target 最近一次抓取是否成功，1 表示成功，0 表示失败。
- Node CPU usage：节点 CPU 使用率，反映节点计算资源压力。
- Pod memory working set：Pod 实际活跃使用内存，适合观察应用内存压力和异常增长。

Prometheus Pull 原理：Prometheus 按配置周期性访问各 target 的 `/metrics` HTTP 端点，把指标拉取到本地时序数据库；Grafana 再通过 PromQL 查询 Prometheus 并展示图表。

### 5.2 附加题2：CI/CD 流水线

任务书要求：代码提交后自动构建镜像、推送 SWR、更新 K8s Deployment；截图展示流水线 Passed 和 K8s Deployment 镜像 Tag 自动更新。

当前实现：

- 使用 GitHub Actions。
- Workflow 文件：`.github/workflows/build-push-deploy.yml`。
- 仓库 Secrets 已配置：`SWR_USERNAME`、`SWR_PASSWORD`、`KUBECONFIG_B64`。
- 最新成功 run：`27764669037`。
- 旧成功截图：`evidence/shot/github_actions_success_27733933670.png`。
- 你补充的新截图：`evidence/shot/github_new_anctions_success.png`。

流水线步骤：

1. Checkout 代码。
2. 登录华为云 SWR。
3. 构建 backend 镜像，tag 为 `${{ github.sha }}` 和 `latest`。
4. 构建 frontend 镜像，tag 为 `${{ github.sha }}` 和 `latest`。
5. 推送镜像到 SWR。
6. 从 GitHub Secret 解码 kubeconfig。
7. 创建/更新 `swr-image-pull` 拉取密钥。
8. 执行 `kubectl set image` 更新 backend/frontend Deployment。
9. `kubectl rollout status` 等待发布完成。

报告可引用证据：

| 证据 | 路径 |
|---|---|
| 最新 Actions API 状态 | `evidence/cicd/github_actions_latest_with_push_retry.txt` |
| GitHub Actions 成功截图 | `evidence/shot/github_new_anctions_success.png` |
| Actions 日志 | `evidence/cicd/github_actions_27733933670_job.log` |
| CI/CD 后公网验证 | `evidence/cicd/post_cicd_endpoint_check.txt`、`evidence/final_verification_after_push_retry.txt` |

CI/CD 概念说明建议：

- CI（持续集成）重点是每次提交后自动构建、测试、检查，尽早发现代码问题。
- CD（持续部署/交付）重点是把通过验证的版本自动发布到目标环境，本项目中体现为自动更新 CCE Deployment 镜像。
- GitOps 核心理念是以 Git 仓库作为期望状态来源，通过自动化工具将集群实际状态同步到 Git 中声明的状态，强调可追踪、可审计、可回滚。

### 5.3 附加题3：C-2 MQTT 边缘计算模拟

任务书要求：K3s + MQTT，模拟边缘传感器数据通过 MQTT Broker 发布到云端 K8s，Redis 存储；分析 MQTT 弱网适用性和云边延迟挑战。

当前实现：

- 在云端 K8s 中部署 MQTT broker。
- 部署 `mqtt-to-redis` bridge，订阅 MQTT topic 并写入 Redis。
- 用 publisher Job 模拟边缘传感器发布消息。
- 采用 QoS0 进行轻量传输验证。

对应文件：

| 内容 | 路径 |
|---|---|
| MQTT broker YAML | `addons/edge-mqtt/cloud/mqtt-broker.yaml` |
| MQTT bridge Dockerfile | `addons/edge-mqtt/cloud/Dockerfile` |
| MQTT bridge Python | `addons/edge-mqtt/cloud/mqtt_to_redis.py` |
| 边缘 publisher | `addons/edge-mqtt/edge/sensor_publisher.py` |
| MQTT 运行证据 | `evidence/edge-mqtt/` |

报告可引用证据：

| 证据 | 路径 |
|---|---|
| broker 状态 | `evidence/edge-mqtt/mqtt_final_broker_status.txt` |
| bridge 状态 | `evidence/edge-mqtt/mqtt_final_bridge_status.txt` |
| publisher 日志 | `evidence/edge-mqtt/mqtt_qos0_publisher_logs.txt` |
| bridge 日志 | `evidence/edge-mqtt/mqtt_qos0_bridge_logs.txt` |
| Redis 中的 MQTT 消息 | `evidence/edge-mqtt/mqtt_qos0_redis_events.txt` |

报告专题建议写法：

- MQTT 报文轻、长连接、主题发布订阅模型适合边缘设备低带宽和弱网场景。
- QoS0 延迟最低但不保证送达；QoS1 提供至少一次送达但可能重复；QoS2 保证仅一次但开销更高。
- 云边协同延迟来自边缘采集、网络传输、Broker 排队、bridge 处理、Redis 写入等环节。
- 本实验用 Kubernetes Job 模拟边缘发布者，没有真正搭建本地 K3s 边缘集群，这是报告中应如实说明的局限；但 MQTT 到云端 K8s 和 Redis 存储链路已完整跑通。

## 6. 证据目录怎么引用

最重要的三个总索引：

| 文件 | 作用 |
|---|---|
| `evidence/CURRENT_COMPLETION_MATRIX.md` | 最终完成矩阵 |
| `evidence/REPORT_EVIDENCE_MAP.md` | 按报告章节映射证据 |
| `evidence/SUBMISSION_CHECKLIST.md` | 提交前检查清单 |

建议写报告时按下面顺序取图：

1. `evidence/screenshots/`：华为云控制台和早期操作截图。
2. `evidence/shot/`：CloudShell、HPA、Grafana、GitHub Actions 截图。
3. `evidence/*.txt`：最终 kubectl、curl、资源状态日志。
4. `evidence/spark_operator/`：Spark A-0 严格证据。
5. `evidence/spark_perf_compare/`：A-3 性能对比。
6. `evidence/edge-mqtt/`：附加题 3 MQTT 证据。
7. `evidence/monitoring/`：附加题 1 监控证据。
8. `evidence/cicd/`：附加题 2 CI/CD 证据。

## 7. 报告推荐结构

报告可以按任务书结构写：

1. 封面。
2. 小组成员与分工。
3. 华为云环境信息。
4. 第一部分：云计算平台搭建。
5. 第二部分：Spark 大数据分析。
6. 附加题1：监控系统。
7. 附加题2：CI/CD 流水线。
8. 附加题3：MQTT 边缘计算模拟。
9. 问题与解决方案。
10. 总结与收获。
11. 附录：仓库链接、核心 YAML、核心代码片段。

### 7.1 分工建议

可以写成：

| 成员 | 分工 | 比例 |
|---|---|---:|
| 2023112554 陈沛陶 | 华为云资源申请与 CCE/SWR/OBS 创建；K8s 部署调试；证据收集；Spark 和附加题联调 | 60% |
| 2023112557 马志凯 | 报告撰写；结果整理；截图排版；原理分析；Amdahl、Prometheus、MQTT 专题说明 | 40% |

比例可按真实情况修改。

### 7.2 问题与解决方案建议写

可以写 4 个典型问题：

1. WSL 网络慢：通过代理/镜像源解决 Docker、Minikube、pip 等下载问题。
2. SWR 拉取私有镜像 401：在 Kubernetes 中创建 `swr-image-pull` Secret，并 patch Deployment 的 `imagePullSecrets`。
3. CCE LoadBalancer pending/listener empty：等待 ELB 资源创建，必要时重新应用 Service 或检查注解。
4. Spark Operator 海外镜像拉取失败：将 controller 镜像和 Spark wordcount 镜像转推到华为云 SWR，使用内网可达镜像源。

## 8. 当前最终云端状态

最终核验文件：`evidence/final_verification_after_push_retry.txt`。

关键结论：

- backend、frontend、redis、mqtt-broker、mqtt-to-redis 均 Running。
- monitoring 命名空间中的 Prometheus/Grafana 均 Running。
- spark-operator controller Running。
- SparkApplication `wordcount-demo` Completed。
- Driver Pod 和 Executor Pod Completed。
- 前端公网 `/api/ping` 返回 ok。
- `/api/visit` 仍能递增，说明 Redis 正常。

## 9. 不要泄露的内容

以下内容不要写进报告、不要发给不需要操作云资源的人、不要提交 GitHub：

- `cloud-course-2023112554-kubeconfig.yaml`
- SWR 用户名对应的密码/Secret Key
- 华为云账号密码
- GitHub token
- 任意 Access Key / Secret Key

如果队友只负责写报告，不需要持有华为云账号。你把当前项目目录打包给他即可，尤其要包含 `evidence/`、`report/`、代码目录和本解说文档。

## 10. 提交前最终动作

提交前还需要人工完成：

1. 写报告并导出 PDF。
2. 检查报告中的截图是否清晰，能看到 Pod 名称、状态、IP、返回值、流水线 Passed 等关键信息。
3. 报告中注明两位成员和分工比例。
4. 邮件主题按老师要求填写：【云计算课设】学号_姓名。
5. 邮件正文附代码仓库链接：[PT0822/CloudExperiment](https://github.com/PT0822/CloudExperiment)。
6. 附上 PDF，不要附 kubeconfig 或任何密钥。

## 11. 实验结束后的资源清理建议

在报告写完、截图确认无误、老师不再要求现场演示后，再清理资源。

先清理 Kubernetes 应用资源：

```bash
kubectl delete -f k8s/ --ignore-not-found
kubectl delete sparkapplication wordcount-demo --ignore-not-found
kubectl delete job spark-douban-analysis spark-perf-1 spark-perf-2 edge-mqtt-publisher --ignore-not-found
kubectl delete -f addons/edge-mqtt/cloud/ --ignore-not-found
```

如果确认不再需要监控和 Spark Operator：

```bash
helm uninstall kps -n monitoring || true
helm uninstall spark-operator -n spark-operator || true
kubectl delete ns monitoring spark-operator --ignore-not-found
```

最后在华为云控制台释放：

- CCE 节点/集群。
- LoadBalancer/ELB。
- EVS 云硬盘/PVC 对应磁盘。
- OBS 中不再需要的数据。
- SWR 镜像可按需保留到提交结束后再删。

不要在报告提交前清理，因为清理后部分截图很难补。

## 12. 任务书第六章与报告质量逐项核对

这一节按任务书第六章和报告质量 10 分逐项核对，写报告时可以直接作为防漏清单。

### 12.1 报告结构必须覆盖

| 任务书要求 | 报告中怎么写 | 当前项目证据 |
|---|---|---|
| 封面 | 写课程名、学号、姓名、班级、日期；小组报告还要写成员与分工比例 | 本指南第 7.1 节给了分工模板 |
| 华为云环境信息 | 写账号 Region、CCE 集群版本、节点规格；截图可代替长文字 | `evidence/screenshots/集群创建并运行*.png`、`evidence/01_cce_nodes_ready.png`、`evidence/final_verification_after_push_retry.txt` |
| 第一部分实验记录 | 按任务 1-6 分小节，每节包含操作步骤摘要、关键截图、问题与解决方案 | `k8s/`、`app/`、`evidence/04_public_api_k8s_resources.txt`、`evidence/05_hpa_metrics_verified.txt`、`evidence/shot/` |
| 第二部分实验记录 | 写方向 A：A-0 到 A-3；包含代码说明、运行截图、性能图表和 Amdahl 分析 | `spark/`、`evidence/07_spark_analysis_result.txt`、`evidence/spark_operator/`、`evidence/spark_perf_compare/` |
| 总结与收获 | 不少于 200 字，要有具体技术挑战和量化结果，不要空泛 | 可引用 API 访问、Redis 持久化、HPA、Spark 执行时间、CI/CD run id 等数据 |
| 附录 | 放全部修改后的 YAML、Dockerfile、核心 Python 代码，或放 GitHub 仓库链接并列出关键文件 | GitHub 仓库：[PT0822/CloudExperiment](https://github.com/PT0822/CloudExperiment)，文件清单见第 13 节 |

### 12.2 报告质量 10 分防漏检查

| 评分项 | 分值 | 报告检查动作 |
|---|---:|---|
| 格式规范 | 2 | 封面必须有学号/姓名/班级；全文字体统一；所有图和表都编号并有标题，例如“图 3-2 backend 与 frontend Pod 状态”。 |
| 内容完整 | 4 | 任务 1-6、A-0 到 A-3、三个附加题分别成节；不能只贴截图，每节要写 3-6 句话说明做了什么、为什么这样做、结果是否达标。 |
| 截图质量 | 2 | 截图必须看得清 Pod 名称、状态、时间、公网 IP、返回值、GitHub Actions Passed 等关键字段；不要使用空白或加载中的截图。 |
| 分析深度 | 2 | 总结不少于 200 字；HPA、Amdahl、Prometheus、MQTT 至少各给出 1 个具体分析点；尽量引用数字，如副本数、耗时、访问次数、CPU 利用率。 |

### 12.3 推荐报告目录

1. 封面
2. 小组成员与分工
3. 华为云实验环境
4. 第一部分：云计算平台搭建
5. 第二部分：Spark 大数据分析
6. 附加题 1：Prometheus/Grafana 监控系统
7. 附加题 2：GitHub Actions CI/CD 流水线
8. 附加题 3：MQTT 云边消息链路模拟
9. 关键问题与解决方案
10. 总结与收获
11. 附录：代码仓库、YAML、Dockerfile、核心 Python 代码

### 12.4 第一部分任务 1-6 写作要点

| 任务 | 报告必须写到 | 代码/配置 | 证据建议 |
|---|---|---|---|
| 任务 1 应用容器化 | 后端多阶段构建、额外 Python 包、前端首页含学号姓名、本地 docker compose 联调、镜像推送 SWR | `app/backend/Dockerfile.backend`、`app/backend/requirements.txt`、`app/frontend/Dockerfile.frontend`、`app/docker-compose.yml` | `evidence/screenshots/2个镜像F&B.png`、`evidence/screenshots/前端证明*.png`、GitHub Actions 构建日志 |
| 任务 2 CCE 集群 | Region 为 `cn-north-4`；CCE 版本 `v1.33.10-r0-33.0.24`；2 个 Worker 节点 Ready；节点规格按控制台截图写 | 控制台创建，无 YAML | `evidence/01_cce_nodes_ready.png`、`evidence/screenshots/集群创建并运行*.png` |
| 任务 3 应用部署 | backend 副本 2、Redis 副本 1、资源 requests/limits、SWR 镜像、ConfigMap/Secret、Service 暴露 | `k8s/01-configmap-secret.yaml`、`03-redis.yaml`、`04-backend.yaml`、`07-services.yaml` | `evidence/04_public_api_k8s_resources.txt`、`evidence/final_verification_after_push_retry.txt` |
| 任务 4 持久化存储 | Redis PVC `Bound`；删除 Redis Pod 后 `/api/visit` 计数继续递增，证明数据未丢 | `k8s/02-pvc.yaml`、`k8s/03-redis.yaml` | `evidence/04_public_api_k8s_resources.txt`、`evidence/shot/cloudshell_资源状态_*.png` |
| 任务 5 ConfigMap Volume | Nginx 配置通过 ConfigMap Volume 挂载到 `/etc/nginx/conf.d/default.conf`；说明 Volume 与 envFrom 区别 | `k8s/05-frontend-nginx-configmap.yaml`、`k8s/06-frontend.yaml` | `evidence/shot/cloudshell_ConfigMap更新证据_*.png` |
| 任务 6 HPA | metrics 可用；HPA min=1 max=4 targetCPU=60%；压测扩容和停止后缩容；说明采集周期、冷却时间、降本价值 | `k8s/08-hpa.yaml` | `evidence/05_hpa_metrics_verified.txt`、`evidence/shot/cloudshell_HPA*.png` |

### 12.5 第二部分 Spark A 写作要点

| 子任务 | 报告必须写到 | 代码/配置 | 证据建议 |
|---|---|---|---|
| A-0 环境部署 | Spark Operator 安装成功；SparkApplication 提交；Driver 和 Executor Pod 均 Completed；说明使用 SWR 镜像避免公网拉取失败 | `spark/sparkapplication.yaml`、`spark/wordcount.py`、`spark/Dockerfile.wordcount` | `evidence/spark_operator/wordcount_keep_executor_pods_final.txt`、`evidence/spark_operator/wordcount_keep_executor_driver_logs.txt` |
| A-1 数据清洗 | 至少 2 种缺失值处理策略；写清洗前后行数变化 | `spark/analysis.py` | `evidence/07_spark_analysis_result.txt` |
| A-2 SQL 统计 | 4 类查询：GROUP BY、Top-N、时间维度、窗口/Top-N；每个结果写约 50 字解释 | `spark/analysis.py` | `evidence/07_spark_analysis_result.txt`、CloudShell 输出截图 |
| A-3 性能对比 | Pandas vs PySpark 执行时间对比；Spark 1 executor vs 2 executor 或不同并行度对比；给出 Amdahl 量化讨论 | `spark/pandas_compare.py`、`evidence/spark_perf_compare/*.yaml` | `evidence/spark_perf_compare/perf_summary.csv`、`evidence/spark_perf_compare/spark_perf_compare.svg` |

A-0 风险说明：任务书模板写了 `executorInstances=2`、`executorMemory="1g"`。当前 CCE 只有 2 台 2vCPU/4GiB 节点，为避免 Pod 因内存不足 Pending，最终可运行证据使用了较低资源配置，重点证明 Spark Operator、Driver、Executor 全链路跑通。报告里不要隐瞒这一点，写成“受节点资源约束，对示例 wordcount 作业做了资源下调；A-3 另用性能对比实验补充并行度分析”。如果老师非常严格要求模板参数完全一致，最稳妥的补强方式是临时加一台更大节点后按模板参数重跑 A-0 并补截图。

### 12.6 三个附加题写作要点

| 附加题 | 得分点 | 证据建议 | 报告分析必须写 |
|---|---|---|---|
| 监控系统 | Prometheus/Grafana 部署成功；Dashboard 有节点 CPU、Pod 内存等指标 | `evidence/shot/grafana_dashboard_monitoring_viewport_2026-06-18.png`、`evidence/monitoring/monitoring_pods_final.txt` | Prometheus Pull 模型；Grafana 指标含义；CPU/内存指标如何辅助运维 |
| CI/CD | GitHub Actions 阶段 Passed；自动构建、推送 SWR、更新 K8s Deployment | `evidence/shot/github_new_anctions_success.png`、`evidence/cicd/github_actions_latest_with_push_retry.txt`、`evidence/final_verification_after_push_retry.txt` | 持续集成 vs 持续部署；GitOps 核心思想；Secrets 不能进仓库 |
| MQTT 前沿专题 | MQTT Broker、Publisher、云端消费者、Redis 写入链路可运行 | `evidence/edge-mqtt/mqtt_qos0_publisher_logs.txt`、`mqtt_qos0_bridge_logs.txt`、`mqtt_qos0_redis_events.txt` | MQTT 弱网适用性、QoS、云边协同延迟、Redis 存储链路 |

附加题 3 风险说明：任务书 C-2 原文是“本地虚拟机安装 K3s 模拟边缘节点 + MQTT 发布到云端 K8s”。当前项目完成的是 K8s 内的 MQTT 云边链路模拟和 Redis 落库证据，没有本地 K3s 节点截图。报告写作时应如实表述为“云边 MQTT 链路模拟扩展”，不要写成已经完整安装本地 K3s。如果追求附加题 3 更稳，需要额外补一张本地 K3s `kubectl get nodes` 和 Publisher Pod/进程截图。

### 12.7 建议图表编号

| 编号 | 图名建议 | 证据来源 |
|---|---|---|
| 图 1 | SWR 镜像仓库中 backend/frontend 镜像及 Tag | `evidence/screenshots/2个镜像F&B.png` 或 GitHub Actions 日志 |
| 图 2 | CCE 集群节点 Ready 与 Kubernetes 版本 | `evidence/01_cce_nodes_ready.png` |
| 图 3 | backend/frontend/redis Pod 运行状态 | `evidence/04_public_api_k8s_resources.txt` 或 CloudShell 截图 |
| 图 4 | Service 与 ELB 公网 IP | `evidence/04_public_api_k8s_resources.txt` |
| 图 5 | `/api/ping` 和 `/api/visit` 返回结果 | `evidence/final_verification_after_push_retry.txt` |
| 图 6 | Redis PVC Bound | `evidence/04_public_api_k8s_resources.txt` |
| 图 7 | 删除 Redis Pod 后访问次数继续递增 | `evidence/shot/cloudshell_资源状态_*.png` 或最终验证日志 |
| 图 8 | ConfigMap Volume 中 Nginx 配置文件内容 | `evidence/shot/cloudshell_ConfigMap更新证据_*.png` |
| 图 9 | HPA 扩容、缩容和 metrics 输出 | `evidence/shot/cloudshell_HPA*.png`、`evidence/05_hpa_metrics_verified.txt` |
| 图 10 | Spark Operator wordcount Driver/Executor Completed | `evidence/spark_operator/wordcount_keep_executor_pods_final.txt` |
| 图 11 | Spark 豆瓣数据分析输出 | `evidence/07_spark_analysis_result.txt` 或 CloudShell 截图 |
| 图 12 | Spark/Pandas 性能对比图 | `evidence/spark_perf_compare/spark_perf_compare.svg` |
| 图 13 | Grafana Dashboard 监控视图 | `evidence/shot/grafana_dashboard_monitoring_viewport_2026-06-18.png` |
| 图 14 | GitHub Actions CI/CD 全部成功 | `evidence/shot/github_new_anctions_success.png` |
| 图 15 | MQTT Publisher、Bridge 和 Redis events | `evidence/edge-mqtt/mqtt_qos0_*.txt` |
| 图 16 | 资源清理前后对比 | 清理前后自行截图，见第 14 节 |

## 13. 附录准备清单

任务书允许“全部修改后的 YAML 文件、Dockerfile 和核心 Python 代码”或者 GitHub 仓库链接。建议报告附录采用“仓库链接 + 关键文件索引 + 代表性代码片段”的方式，避免 PDF 过长。

### 13.1 GitHub 仓库链接

仓库链接：[https://github.com/PT0822/CloudExperiment](https://github.com/PT0822/CloudExperiment)

报告中建议写：完整代码、YAML、Dockerfile、CI/CD workflow 和证据文件均已放入仓库；正文只展示核心片段，附录列出文件路径。

### 13.2 YAML 文件清单

| 类型 | 文件 |
|---|---|
| 第一部分 K8s 基础资源 | `k8s/01-configmap-secret.yaml`、`k8s/02-pvc.yaml`、`k8s/03-redis.yaml`、`k8s/04-backend.yaml`、`k8s/05-frontend-nginx-configmap.yaml`、`k8s/06-frontend.yaml`、`k8s/07-services.yaml`、`k8s/08-hpa.yaml` |
| Spark 作业 | `spark/spark-job.yaml`、`spark/sparkapplication.yaml`、`evidence/spark_operator/sparkapplication_wordcount_keep_executor.yaml` |
| Spark 性能对比 | `evidence/spark_perf_compare/spark-perf-1.yaml`、`evidence/spark_perf_compare/spark-perf-2.yaml` |
| 监控附加题 | `addons/monitoring/kube-prometheus-stack-values.yaml`、`evidence/monitoring/kps-light-values.yaml`、`evidence/monitoring/course-prometheus.yaml` |
| MQTT 附加题 | `addons/edge-mqtt/cloud/mqtt-broker.yaml`、`addons/edge-mqtt/cloud/mqtt-to-redis.yaml`、`evidence/edge-mqtt/edge-mqtt-publisher-qos0.yaml` |
| CI/CD | `.github/workflows/build-push-deploy.yml` |

### 13.3 Dockerfile 清单

| 用途 | 文件 |
|---|---|
| Flask 后端镜像 | `app/backend/Dockerfile.backend` |
| Nginx 前端镜像 | `app/frontend/Dockerfile.frontend` |
| Spark 数据分析镜像 | `spark/Dockerfile.spark` |
| Spark Operator wordcount 镜像 | `spark/Dockerfile.wordcount` |
| MQTT 云端消费者镜像 | `addons/edge-mqtt/cloud/Dockerfile` |

### 13.4 核心 Python 代码清单

| 用途 | 文件 | 报告说明点 |
|---|---|---|
| 后端 API | `app/backend/app.py` | `/api/ping` 健康检查、`/api/visit` 访问计数写入 Redis、环境变量读取 Redis 配置 |
| Spark 清洗与 SQL | `spark/analysis.py` | 缺失值处理、GROUP BY、Top-N、时间维度、窗口函数/Top-N 查询 |
| Pandas 基线 | `spark/pandas_compare.py` | 与 PySpark 对照，形成 A-3 性能分析 |
| Spark Operator 示例 | `spark/wordcount.py` | 验证 SparkApplication、Driver、Executor 全链路 |
| MQTT 云端消费者 | `addons/edge-mqtt/cloud/mqtt_to_redis.py` | 订阅 MQTT topic 并写入 Redis |
| MQTT 边缘发布者 | `addons/edge-mqtt/edge/sensor_publisher.py` | 模拟传感器数据发布 |

### 13.5 附录写法建议

附录不要把所有文件全文复制到 PDF，建议这样写：

1. 先给 GitHub 仓库链接。
2. 列出上面 3 张文件清单表。
3. 贴 4 段代表性代码：`Dockerfile.backend` 多阶段构建、`k8s/04-backend.yaml` resources/env/secret、`spark/analysis.py` 核心 SQL、`.github/workflows/build-push-deploy.yml` CI/CD steps。
4. 说明 `kubeconfig`、SWR 密码、GitHub Secrets 不进入附录，也不提交仓库。

## 14. 云资源清理清单与截图说明

单独清单文件：云资源清理清单_截图说明.md。

只在报告截图已经齐全、PDF 初稿已经完成、老师不再要求现场演示后清理。清理前后建议各截一组图，证明资源不是误删前没有完成，而是实验完成后主动释放。

### 14.1 清理前截图

清理前建议保存这些截图或命令输出：

1. CCE 集群概览：集群名称、Region、版本、运行中状态。
2. CCE 节点列表：2 个 Worker 节点 Ready，含节点 IP、版本。
3. K8s 工作负载：`kubectl get pods -A -o wide`，能看到 backend、frontend、redis、monitoring、spark-operator、MQTT 等关键资源。
4. Service/ELB：`kubectl get svc -A`，能看到 `frontend-svc` 或对外 Service 的公网 IP。
5. PVC/EVS：`kubectl get pvc`，能看到 `redis-data-pvc Bound`。
6. HPA：`kubectl get hpa`，能看到 `backend-hpa`。
7. Spark：`kubectl get sparkapplication` 和 wordcount Driver/Executor Completed 证据。
8. OBS：桶和数据集对象截图。
9. SWR：backend/frontend/spark/mqtt 镜像与 Tag 截图。
10. GitHub Actions：最新成功流水线截图。

### 14.2 Kubernetes 资源清理命令

下面命令在 CloudShell 或已配置 kubeconfig 的终端执行。确认报告完成后再执行。

```bash
# 1. 清理第一部分 Web 应用资源
kubectl delete -f k8s/ --ignore-not-found

# 2. 清理 Spark 作业和性能对比 Job
kubectl delete sparkapplication wordcount-demo --ignore-not-found
kubectl delete job spark-douban-analysis spark-perf-1 spark-perf-2 --ignore-not-found

# 3. 清理 MQTT 附加题资源
kubectl delete -f addons/edge-mqtt/cloud/ --ignore-not-found
kubectl delete job edge-mqtt-publisher --ignore-not-found

# 4. 如 Service 未被 YAML 删除，显式清理可能产生 ELB 的 Service
kubectl delete svc frontend-svc backend-lb-svc backend-svc redis-svc mqtt-broker --ignore-not-found

# 5. 清理监控与 Spark Operator
helm uninstall kps -n monitoring || true
helm uninstall spark-operator -n spark-operator || true
kubectl delete ns monitoring spark-operator --ignore-not-found

# 6. 最后清理 PVC；这一步会导致持久化数据不可恢复
kubectl delete pvc redis-data-pvc spark-data-pvc --ignore-not-found
```

### 14.3 华为云控制台资源清理

Kubernetes 对象删完后，还要在华为云控制台核对这些资源，防止继续扣费：

| 资源 | 控制台位置 | 清理动作 | 截图说明 |
|---|---|---|---|
| CCE 集群/节点 | 云容器引擎 CCE | 删除集群，或至少删除节点池/节点 | 截图显示集群已删除，或节点数为 0/无运行节点 |
| ELB/负载均衡 | 弹性负载均衡 ELB | 删除由 `LoadBalancer` Service 创建的监听器、后端服务器组、负载均衡实例 | 截图显示无相关 `frontend-svc` / `backend` 负载均衡 |
| EIP | 弹性公网 IP | 释放未绑定或实验使用的公网 IP | 截图显示公网 IP 已释放或无绑定资源 |
| EVS 云硬盘 | 云硬盘 EVS | 删除 PVC 遗留的云硬盘；如果有快照也检查 | 截图显示无 `redis-data-pvc` 对应磁盘 |
| OBS 桶 | 对象存储 OBS | 删除数据集对象；如不再需要可删除桶 | 截图显示桶为空或已删除 |
| SWR 镜像 | 容器镜像服务 SWR | 可保留到评分结束；若确定不用再删镜像和组织 | 截图显示镜像保留或清理结果 |
| 费用中心 | 费用中心/资源包 | 检查近期扣费和活跃资源 | 截图显示无异常持续扣费 |

### 14.4 清理后验证命令

如果集群还没删，可以执行：

```bash
kubectl get pods -A
kubectl get svc -A
kubectl get pvc -A
helm list -A
```

期望结果：只剩系统组件，或已经无法连接集群但控制台显示集群删除中/已删除。清理后的截图可以放在报告最后的“资源释放说明”或附录，不一定放正文。

## 15. 最终防漏结论

当前项目已经足够支撑一份完整报告：第一部分 1-6、第二部分 Spark A-0 到 A-3、附加题 1 监控、附加题 2 CI/CD、附加题 3 MQTT 链路均有代码和证据。报告撰写时重点注意三件事：

1. 不能暴露 `kubeconfig`、SWR 密码、GitHub token、华为云账号密码。
2. A-0 的资源参数和附加题 3 的 K3s 实机要求存在保守风险，要按第 12.5 和 12.6 节如实说明，或额外补强截图。
3. 截图必须编号、加标题，并配文字解释；不要只堆图。


## 16. 任务书最终核对与报告写作兜底清单

核对时间：2026-06-19 12:11:38 +08:00
核对依据：`课程设计任务书-final.pdf` 第五章评分标准、第六章实验报告结构要求、附录模板要求，以及当前项目目录和 `evidence/` 证据目录。

### 16.1 按任务书总分结构核对

| 模块 | 任务书要求 | 当前项目是否能支撑报告 | 报告必须呈现的证据 |
|---|---|---|---|
| 第一部分 50 分 | 任务 1-6 全部完成 | 可以支撑 | 每个任务至少 1 张截图或 1 段命令输出；重点是 SWR、CCE 节点、Deployment/Service、PVC、ConfigMap Volume、HPA |
| 第二部分 40 分 | 选择 A 或 B；本项目选择方向 A Spark | 可以支撑 | A-0 SparkApplication、Driver/Executor Completed；A-1 清洗；A-2 四类 SQL；A-3 性能图和 Amdahl 分析 |
| 报告质量 10 分 | 格式规范、内容完整、截图清晰、分析深度 | 取决于最终 PDF | 每张图编号和标题；不要只有截图；总结不少于 200 字且引用量化数据 |
| 附加题最高 +15 分 | 监控、CI/CD、前沿专题各 +5 | 证据基本齐全 | 监控 Dashboard、GitHub Actions Passed、MQTT 发布/消费/Redis 落库证据 |

### 16.2 第一部分 1-6 得分点逐项检查

| 任务 | 分值 | 任务书评分点 | 当前证据 | 报告写作注意 |
|---|---:|---|---|---|
| 任务 1 应用容器化 | 10 | 多阶段 Dockerfile；新增 Python 包；前端含学号姓名；SWR 镜像名和 Tag；docker compose 联调 | `app/backend/Dockerfile.backend`、`app/backend/requirements.txt`、`app/frontend/static/index.html`、`app/docker-compose.yml`、`evidence/screenshots/2个镜像F&B.png` | 报告中明确写 `requests` 是自选新增包；截图要能看到镜像 Tag；本地联调若截图不够清晰，可引用 compose 文件和前端/API 访问截图补充 |
| 任务 2 CCE 集群 | 8 | 至少 2 个 Worker Ready；截图含 VERSION；集群版本 >= 1.27 | `evidence/01_cce_nodes_ready.png`、`evidence/screenshots/集群创建并运行*.png` | 环境信息章节要写 Region `cn-north-4`、CCE 版本 `v1.33.10-r0-33.0.24`、节点规格 `c9.large.2 2vCPU/4GiB` |
| 任务 3 应用部署 | 12 | backend 副本 2；Redis 副本 1；resources；SWR 镜像；后端 LoadBalancer 可访问 `/api/ping`；Redis ClusterIP；ConfigMap+Secret | `k8s/01-configmap-secret.yaml` 到 `k8s/08-hpa.yaml`、`evidence/final_verification_after_push_retry.txt` | Secret 不要贴明文；可以贴 YAML 片段证明 `secretKeyRef`，不要贴真实密码 |
| 任务 4 Redis 持久化 | 10 | PVC Bound；删除 Pod 重建后数据仍在，最好有前/删/后对比 | `k8s/02-pvc.yaml`、`k8s/03-redis.yaml`、`evidence/04_public_api_k8s_resources.txt`、`evidence/shot/cloudshell_资源状态_*.png` | 报告要写清楚 `/api/visit` 计数从 3 到 4，说明 Redis Pod 重建后 PVC 数据仍保留 |
| 任务 5 ConfigMap Volume | 5 | ConfigMap 以 Volume 挂载；exec 验证文件更新；说明 Volume vs envFrom | `k8s/05-frontend-nginx-configmap.yaml`、`k8s/06-frontend.yaml`、`evidence/shot/cloudshell_ConfigMap更新证据_*.png` | 必须解释：Volume 适合多行配置文件，envFrom 适合简单键值环境变量 |
| 任务 6 HPA | 5 | Pod 数从 1 增加截图；停止压测后缩容截图；分析延迟/冷却/降本 | `k8s/08-hpa.yaml`、`evidence/05_hpa_metrics_verified.txt`、`evidence/shot/cloudshell_HPA*.png` | 报告中不要只贴 HPA 状态，要解释 metrics-server 采集周期和 HPA 缩容稳定窗口 |

### 16.3 第二部分 Spark A 得分点逐项检查

| 子任务 | 分值 | 任务书评分点 | 当前证据 | 报告写作注意 |
|---|---:|---|---|---|
| A-0 环境部署 | 10 | SparkApplication YAML 参数正确；Driver + Executor Pod Completed | `spark/sparkapplication.yaml`、`spark/wordcount.py`、`evidence/spark_operator/wordcount_keep_executor_pods_final.txt`、`wordcount_keep_executor_driver_logs.txt` | 当前证据能证明 Spark Operator 全链路跑通；如老师严格看模板资源参数，报告要说明因 2 台 2vCPU/4GiB 节点资源限制做了低资源可运行配置 |
| A-1 数据清洗 | 10 | 2 种缺失值策略；清洗前后行数 | `spark/analysis.py`、`evidence/07_spark_analysis_result.txt` | 把 `dropna` 与 `fillna` 分开说明，且写为什么关键数值字段不能随便填充 |
| A-2 Spark SQL | 15 | 4 个查询含 GROUP BY、Top-N、时间维度、JOIN/窗口；每个截图 + 约 50 字分析 | `spark/analysis.py`、`spark_analysis_result.txt`、`evidence/07_spark_analysis_result.txt` | 方向 A 中本项目使用窗口函数满足 JOIN/窗口要求；每个 Query 下必须写结果解读，不要只贴表格 |
| A-3 性能分析 | 5 | Pandas vs PySpark 对比图；Amdahl 量化分析 | `spark/pandas_compare.py`、`evidence/spark_perf_compare/perf_summary.csv`、`spark_perf_compare.svg` | 写出 Spark 固定开销、调度开销、Shuffle/序列化开销导致非线性加速；至少引用一次实际耗时 |

### 16.4 附加题得分点逐项检查

| 附加题 | 分值 | 任务书评分点 | 当前证据 | 报告写作注意 |
|---|---:|---|---|---|
| 监控系统 | +5 | Prometheus + Grafana 部署；Dashboard 有节点 CPU 折线和 Pod 内存柱状；说明 Pull 原理和 3 个指标 | `evidence/shot/grafana_dashboard_monitoring_viewport_2026-06-18.png`、`evidence/monitoring/*` | 指标建议写 `up`、节点 CPU、Pod 内存；说明 Prometheus 主动定时拉取 `/metrics` |
| CI/CD | +5 | 代码提交后自动构建、推送 SWR、更新 K8s Deployment；截图全部 Passed；说明 CI/CD 与 GitOps | `.github/workflows/build-push-deploy.yml`、`evidence/shot/github_new_anctions_success.png`、`evidence/cicd/*` | 截图需能看到流水线成功；镜像 Tag 更新验证可引用 Actions 日志和最终 K8s 状态 |
| MQTT 前沿专题 | +5 | C-2 K3s + MQTT；实验可运行；报告不少于 1500 字专题内容；有批判性分析 | `addons/edge-mqtt/`、`evidence/edge-mqtt/mqtt_qos0_*.txt` | 当前实现是 K8s 内 MQTT 云边链路模拟，不是完整本地 K3s。报告可作为“云边模拟链路”写，但若追求满分，最好补本地 K3s 节点截图；否则要如实说明局限 |

### 16.5 报告 PDF 结构模板

最终 PDF 建议使用下面结构，基本能覆盖任务书第六章全部要求：

1. 封面：课程名、题目、小组成员、学号、班级、日期。
2. 小组分工：2023112554 陈沛陶、2023112557 马志凯；写清楚容器化、K8s、Spark、附加题、报告分工。
3. 华为云环境信息：Region、CCE 版本、节点规格、SWR 组织、OBS 桶、ELB 公网 IP；用截图代替长描述。
4. 第一部分实验记录：任务 1 到任务 6，每节包含“操作摘要、关键配置、截图/日志、问题与解决”。
5. 第二部分 Spark 实验记录：A-0 到 A-3，包含代码说明、结果截图、性能图和 Amdahl 分析。
6. 附加题 1：监控系统，写 Prometheus/Grafana 部署、Dashboard、指标解释。
7. 附加题 2：CI/CD，写 GitHub Actions 流水线、Secrets 管理、Deployment 更新验证、CI/CD/GitOps 概念。
8. 附加题 3：MQTT 云边模拟，写架构、发布/订阅/Redis 落库、QoS、弱网适用性、云边延迟挑战；专题内容不少于 1500 字更稳。
9. 问题与解决方案：镜像拉取、ELB pending、metrics 延迟、Spark 资源不足、账号冻结导致清理受限。
10. 总结与收获：不少于 200 字，引用具体数据，不写空泛感想。
11. 资源清理说明：列清理前截图、已删除 CCE、冻结资源无法手动退订及后续处理。
12. 附录：GitHub 仓库链接、YAML/Dockerfile/Python 核心文件清单、代表性代码片段。

### 16.6 附录最稳写法

任务书允许“全部修改后的 YAML 文件、Dockerfile 和核心 Python 代码（或 GitHub 仓库链接）”。建议不要把所有文件全文塞进 PDF，而是这样写：

- 先放 GitHub 仓库链接：`https://github.com/PT0822/CloudExperiment`。
- 再放“关键文件索引表”，包含 `k8s/`、`spark/`、`addons/`、`.github/workflows/`、`app/`。
- 贴 4 到 6 个核心片段，每个片段 20 到 60 行即可：
  - `app/backend/Dockerfile.backend`：多阶段构建。
  - `k8s/04-backend.yaml`：副本数、SWR 镜像、resources、ConfigMap/Secret 注入。
  - `k8s/06-frontend.yaml` + `k8s/05-frontend-nginx-configmap.yaml`：ConfigMap Volume 挂载。
  - `spark/analysis.py`：清洗策略和 4 个 SQL 查询。
  - `.github/workflows/build-push-deploy.yml`：构建、推送、kubectl 更新 Deployment。
  - `addons/edge-mqtt/cloud/mqtt_to_redis.py`：MQTT 订阅并写入 Redis。
- 不要在附录放 `cloud-course-2023112554-kubeconfig.yaml`、SWR 密码、GitHub Secrets、华为云账号信息。

### 16.7 附录文件索引可直接复制

| 类别 | 文件路径 | 报告说明 |
|---|---|---|
| 后端镜像 | `app/backend/Dockerfile.backend` | Flask 后端多阶段 Dockerfile，builder 阶段安装依赖，runtime 阶段复制依赖和代码 |
| 前端镜像 | `app/frontend/Dockerfile.frontend` | Nginx 静态页面镜像 |
| 本地联调 | `app/docker-compose.yml` | 本地 backend、frontend、redis 联调 |
| 后端代码 | `app/backend/app.py` | `/api/ping`、`/api/visit`、`/api/redis-test` |
| 基础 K8s | `k8s/01-configmap-secret.yaml` 到 `k8s/08-hpa.yaml` | ConfigMap、Secret、PVC、Redis、backend、frontend、Service、HPA |
| Spark 分析 | `spark/analysis.py` | 数据清洗、Spark SQL、窗口函数 |
| Spark 性能 | `spark/pandas_compare.py`、`evidence/spark_perf_compare/*.yaml` | Pandas 和 PySpark 性能对比 |
| Spark Operator | `spark/sparkapplication.yaml`、`spark/wordcount.py` | SparkApplication 和 wordcount 验证 |
| 监控 | `addons/monitoring/kube-prometheus-stack-values.yaml`、`evidence/monitoring/course-prometheus.yaml` | Prometheus/Grafana 配置 |
| CI/CD | `.github/workflows/build-push-deploy.yml` | GitHub Actions 构建、推送、部署 |
| MQTT | `addons/edge-mqtt/cloud/*.yaml`、`addons/edge-mqtt/cloud/mqtt_to_redis.py`、`addons/edge-mqtt/edge/sensor_publisher.py` | MQTT broker、bridge、publisher |

### 16.8 资源清理写入报告的建议措辞

报告最后可加入一小节“资源释放与费用控制”：

> 实验完成并完成截图留证后，小组在华为云控制台执行资源清理。CCE 集群 `cloud-course-2023112554` 已提交删除，并在集群列表中不再显示。由于账号/资源处于冻结状态，部分 ELB 等资源在控制台提示“已冻结，不能执行退订操作”，页面显示将在冻结倒计时结束后自动删除。我们保留了删除提交、删除中、删除后列表以及冻结无法退订的截图，作为实验结束后主动释放资源和费用控制的证据。

如果报告提交前资源仍显示冻结等待自动删除，不要写“所有资源均已手动删除”，应写“CCE 已删除；冻结状态下的 ELB/EIP 等按控制台提示等待自动释放，并已截图留证”。

### 16.9 最后风险项

| 风险 | 是否影响基础得分 | 建议处理 |
|---|---|---|
| 附加题 3 原文要求本地 K3s，本项目主要完成 K8s 内 MQTT 云边链路模拟 | 不影响 100 分基础分；可能影响附加题 3 满分 | 报告如实说明局限；如果还有时间，补本地 K3s 截图最稳 |
| A-0 SparkApplication 为适配小规格节点做了低资源配置 | 通常不影响全链路证明；严格按模板可能被扣细节 | 报告写明资源约束和原因；引用 Driver/Executor Completed 证明功能达标 |
| 清理阶段账号/资源冻结，ELB/EIP 无法手动退订 | 不影响实验完成；影响“清理已彻底完成”的表述 | 使用截图证明 CCE 已删、ELB 冻结无法退订并等待自动释放 |
